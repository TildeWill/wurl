%table{:id => "category_#{category.id}"}
  %caption
    %strong= category.name
    = link_to( 'Destroy', category, :confirm => 'Are you sure?', :method => :delete, :remote => true, :class => 'destroy button')
    = link_to('Edit', edit_category_path(category), :class => 'edit button')
    %p=category.description
  %thead
    %tr
      %th Resource
      %th Description
  %tbody