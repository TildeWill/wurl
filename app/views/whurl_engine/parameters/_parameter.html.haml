%tr{:id => "parameter_#{parameter.id}"}
  %td
    %strong= parameter.name
    %br
    = parameter.required? ? "required" : "optional"
    = link_to('', edit_parameter_path(parameter), :remote => true, :class => 'edit button')
    = link_to( '', parameter, :confirm => 'Are you sure?', :method => :delete, :remote => true, :class => 'destroy button')

  %td
    = parameter.description
    %div
      Example Value:
      = parameter.example_value
